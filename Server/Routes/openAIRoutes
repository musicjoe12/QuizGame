const express = require('express');
const router = express.Router();
const { generateQuestions } = require('../Controllers/OpenAI');

router.post('/ai/generate-questions', generateQuestions);

module.exports = router;